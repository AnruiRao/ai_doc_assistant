# V3 实测问题集

每次改动后，用此列表中的 query 重新测试，肉眼对比回答质量。

## 测试 Query

| # | Query | 质量 | RAGAS | 类型 | 问题描述 |
|---|-------|------|-------|------|---------|
| 1 | V3 主要做什么？ | ❌→✅ | F=0.143 R=0.736 | 宽泛查询 | chunk_size 500→1000 后从胡编变为准确，仍受上下文碎片影响 |
| 2 | chunk_text 和 recursive_split 有什么区别？ | ✅ | F=0.333 R=0.908 | 语义匹配 | 回答全面，但部分细节上下文未覆盖 |
| 3 | Agent 的 run 方法接受哪些参数？ | ⚠️→✅ | F=0.000 R=0.805 | 代码检索 | 答案准确但上下文完全无关，靠模型知识回答 |
| 4 | 文档切分时怎么避免把一段话从中间切断？ | ✅ | F=0.270 R=0.995 | 语义匹配 | 回答准确，上下文碎片化导致 F 不高 |
| 5 | 向量库存在哪个目录下？ | ✅ | F=0.000 R=0.874 | 精确查询 | 答案准确但无可信上下文 |
| 6 | 项目为什么选择 Chroma 而不是其他向量数据库？ | ✅ | F=0.114 R=0.000 | 决策原因 | R=0 疑似评分类失败 |
| 7 | V2 工程化阶段改了哪些核心文件？ | ✅ | F=0.509 R=0.863 | 多文档聚合 | 回答准确 |
| 8 | 为什么项目不直接用 LangChain？ | ✅ | F=0.500 R=0.827 | 决策原因 | 回答准确 |
| 9 | 为什么 embedding 模型选了 paraphrase-multilingual-MiniLM-L12-v2？ | ✅ | F=0.789 R=0.839 | 决策原因 | 检索质量较好 |
| 10 | 支持多用户同时使用吗？ | ❌→⚠️ | F=0.958 R=0.912 | 空检索 | 准确讲了架构，但未提"无用户认证"的局限 |
| 11 | 项目有没有 Docker 部署方案？ | ⚠️→✅ | F=0.176 R=0.752 | 空检索 | 滑动窗口后准确回答，但计划信息零散 |
| 12 | VectorStore 类有哪些核心方法？ | ✅ | F=0.600 R=0.919 | 代码检索 | 能正确检索加回答 |
| 13 | ReAct Agent 的循环流程是怎样的？ | ✅ | F=0.083 R=0.907 | 概念理解 | 上下文无实际代码/流程图仍能准确回答 |
| 14 | RAG Tool 的 save 和 search 模式分别做了什么？ | ✅ | F=0.000 R=0.896 | 功能描述 | 答案准确但上下文均无关 |
| 15 | Chunker 和 VectorStore 是在哪个环节被调用的？ | ✅ | F=1.000 R=0.846 | 跨文件流程 | ✅ 完美命中 |
| 16 | 文档从上传到可被检索，经历了哪些步骤？ | ⚠️→✅ | F=0.231 R=0.811 | 全流程 | 回答准确，上下文散乱未充分覆盖 |
| 17 | 如果上传一个非常大的文件，系统会怎么处理？ | ⚠️ | F=0.154 R=0.920 | 系统能力 | chunking 回答准确，但未指出内存读取局限 |
| 18 | chunk_size 和 chunk_overlap 会影响检索结果吗？怎么影响的？ | ✅ | F=0.650 R=0.895 | 推理类 | 回答准确 |
| 19 | 项目异常体系中有哪些可重试的异常？ | ✅ | F=0.091 R=0.803 | 细节考察 | 上下文只有"Phase 1"标题 |
| 20 | Streamlit 瘦客户端模式下 UI 与后端是如何通信的？ | ✅ | F=1.000 R=0.868 | 架构理解 | ✅ 完美命中 |

## 改动记录

| 日期 | 改动 | 影响 |
|------|------|------|
| 2026-06-25 | chunk_size 500→1000, overlap 50→100 | query #1 从胡编变为准确 |
| 2026-06-25 | Agent 搜索次数上限加入代码硬拦截（max_search=3） | 避免模型无限循环搜索 |
| 2026-06-25 | 滑动窗口上下文（每个命中 chunk 带回前后各 2 个相邻 chunk） | #11、#16 从 ⚠️→✅；#3（代码检索）一并改善 |
| 2026-06-25 | 搭建 RAGAS 评估 pipeline（`scripts/evaluate_rag.py`） | 出 baseline 前 6 条 |
| 2026-06-26 | 修复 RAGAS 断点续评 Bug（0.0 掩盖评分失败），补全 20 条全量评估 | 20/20 全部出分，baseline F=0.38 R=0.82 |
| 2026-06-28 | V3 实验 A1 Query Rewrite 基线（Agent: qwen3.6-plus-2026-04-02, Judge: 同） | F=0.484 R=0.910. 基线已备份至 `data/eval_scores_v3_baseline.json` |
| 2026-06-28 | **A1 Query Rewrite 开启**（Agent: qwen3.6-plus, Judge: 同） | **F=0.570 R=0.881**. 列举型大幅改善: #12(0.474→0.900), #14(0.643→0.889), #19(0.500→0.900). #13退化(0.735→0.250)疑似过度拆分. 结果已备份至 `data/eval_scores_v3_qr.json` |

## 质量标记说明

- ✅ 回答准确
- ⚠️ 部分偏差或误导性
- ❌ 明显胡编或错误
- → 表示经过改动后从 ❌ 变为 ✅
